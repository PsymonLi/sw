import { Telemetry_queryMetricsQuerySpec_function } from "@sdk/v1/models/generated/telemetry_query";

export interface MetricField {
  name: string,
  description?: string,
  displayName: string,
  units?: string,
  baseType: string,
  jsType: string,
  allowedValues?: string[],
  scaleMin?: number,
  scaleMax?: number,
  tags?: string[],
  scope?: string,
  isTag?: boolean,
  aggregationFunc?: Telemetry_queryMetricsQuerySpec_function,
}

export interface MetricMeasurement {
  name: string,
  description: string,
  displayName: string,
  uiGroup: string
  objectKind?: string,
  // Type of interface, if objectKind is networkInterface
  interfaceType?: string,
  scope?: string,
  fields: MetricField[],
  tags?: string[],
  features?: string[],
}

export let MetricsMetadata: { [key: string]: MetricMeasurement } = {}

export function setMetricsMetadata(val: { [key: string]: MetricMeasurement }) {
  MetricsMetadata = val;
}
