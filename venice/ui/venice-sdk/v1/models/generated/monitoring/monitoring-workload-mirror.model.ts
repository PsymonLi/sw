/**
 * This file is generated by the SwaggerTSGenerator.
 * Do not edit.
*/
/* tslint:disable */
import { Validators, FormControl, FormGroup, FormArray, ValidatorFn } from '@angular/forms';
import { minValueValidator, maxValueValidator, minLengthValidator, maxLengthValidator, required, enumValidator, patternValidator, CustomFormControl, CustomFormGroup } from '../../../utils/validators';
import { BaseModel, PropInfoItem } from '../basemodel/base-model';

import { MonitoringWorkloadMirror_direction,  MonitoringWorkloadMirror_direction_uihint  } from './enums';
import { LabelsSelector, ILabelsSelector } from './labels-selector.model';

export interface IMonitoringWorkloadMirror {
    'direction': MonitoringWorkloadMirror_direction;
    'selectors'?: Array<ILabelsSelector>;
    '_ui'?: any;
}


export class MonitoringWorkloadMirror extends BaseModel implements IMonitoringWorkloadMirror {
    /** Field for holding arbitrary ui state */
    '_ui': any = {};
    /** Rx is towards the workload and tx is from workload. */
    'direction': MonitoringWorkloadMirror_direction = null;
    'selectors': Array<LabelsSelector> = null;
    public static propInfo: { [prop in keyof IMonitoringWorkloadMirror]: PropInfoItem } = {
        'direction': {
            enum: MonitoringWorkloadMirror_direction_uihint,
            default: 'both',
            description:  `Rx is towards the workload and tx is from workload.`,
            required: true,
            type: 'string'
        },
        'selectors': {
            required: false,
            type: 'object'
        },
    }

    public getPropInfo(propName: string): PropInfoItem {
        return MonitoringWorkloadMirror.propInfo[propName];
    }

    public getPropInfoConfig(): { [key:string]:PropInfoItem } {
        return MonitoringWorkloadMirror.propInfo;
    }

    /**
     * Returns whether or not there is an enum property with a default value
    */
    public static hasDefaultValue(prop) {
        return (MonitoringWorkloadMirror.propInfo[prop] != null &&
                        MonitoringWorkloadMirror.propInfo[prop].default != null);
    }

    /**
     * constructor
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    constructor(values?: any, setDefaults:boolean = true) {
        super();
        this['selectors'] = new Array<LabelsSelector>();
        this._inputValue = values;
        this.setValues(values, setDefaults);
    }

    /**
     * set the values for both the Model and the Form Group. If a value isn't provided and we have a default, we use that.
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    setValues(values: any, fillDefaults = true): void {
        if (values && values['_ui']) {
            this['_ui'] = values['_ui']
        }
        if (values && values['direction'] != null) {
            this['direction'] = values['direction'];
        } else if (fillDefaults && MonitoringWorkloadMirror.hasDefaultValue('direction')) {
            this['direction'] = <MonitoringWorkloadMirror_direction>  MonitoringWorkloadMirror.propInfo['direction'].default;
        } else {
            this['direction'] = null
        }
        if (values) {
            this.fillModelArray<LabelsSelector>(this, 'selectors', values['selectors'], LabelsSelector);
        } else {
            this['selectors'] = [];
        }
        this.setFormGroupValuesToBeModelValues();
    }


    protected getFormGroup(): FormGroup {
        if (!this._formGroup) {
            this._formGroup = new FormGroup({
                'direction': CustomFormControl(new FormControl(this['direction'], [required, enumValidator(MonitoringWorkloadMirror_direction), ]), MonitoringWorkloadMirror.propInfo['direction']),
                'selectors': new FormArray([]),
            });
            // generate FormArray control elements
            this.fillFormArray<LabelsSelector>('selectors', this['selectors'], LabelsSelector);
            // We force recalculation of controls under a form group
            Object.keys((this._formGroup.get('selectors') as FormGroup).controls).forEach(field => {
                const control = this._formGroup.get('selectors').get(field);
                control.updateValueAndValidity();
            });
        }
        return this._formGroup;
    }

    setModelToBeFormGroupValues() {
        this.setValues(this.$formGroup.value, false);
    }

    setFormGroupValuesToBeModelValues() {
        if (this._formGroup) {
            this._formGroup.controls['direction'].setValue(this['direction']);
            this.fillModelArray<LabelsSelector>(this, 'selectors', this['selectors'], LabelsSelector);
        }
    }
}

