/**
 * This file is generated by the SwaggerTSGenerator.
 * Do not edit.
*/
/* tslint:disable */
import { Validators, FormControl, FormGroup, FormArray, ValidatorFn } from '@angular/forms';
import { minValueValidator, maxValueValidator, minLengthValidator, maxLengthValidator, required, enumValidator, patternValidator, CustomFormControl, CustomFormGroup } from '../../../utils/validators';
import { BaseModel, PropInfoItem } from '../basemodel/base-model';

import { NetworkPolicerCriteria, INetworkPolicerCriteria } from './network-policer-criteria.model';
import { NetworkPolicerAction, INetworkPolicerAction } from './network-policer-action.model';

export interface INetworkPolicerProfileSpec {
    'criteria'?: INetworkPolicerCriteria;
    'exceed-action'?: INetworkPolicerAction;
    '_ui'?: any;
}


export class NetworkPolicerProfileSpec extends BaseModel implements INetworkPolicerProfileSpec {
    /** Field for holding arbitrary ui state */
    '_ui': any = {};
    /** Traffic Policer Criteria. */
    'criteria': NetworkPolicerCriteria = null;
    /** Traffc Policer Action. */
    'exceed-action': NetworkPolicerAction = null;
    public static propInfo: { [prop in keyof INetworkPolicerProfileSpec]: PropInfoItem } = {
        'criteria': {
            description:  `Traffic Policer Criteria.`,
            required: false,
            type: 'object'
        },
        'exceed-action': {
            description:  `Traffc Policer Action.`,
            required: false,
            type: 'object'
        },
    }

    public getPropInfo(propName: string): PropInfoItem {
        return NetworkPolicerProfileSpec.propInfo[propName];
    }

    public getPropInfoConfig(): { [key:string]:PropInfoItem } {
        return NetworkPolicerProfileSpec.propInfo;
    }

    /**
     * Returns whether or not there is an enum property with a default value
    */
    public static hasDefaultValue(prop) {
        return (NetworkPolicerProfileSpec.propInfo[prop] != null &&
                        NetworkPolicerProfileSpec.propInfo[prop].default != null);
    }

    /**
     * constructor
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    constructor(values?: any, setDefaults:boolean = true) {
        super();
        this['criteria'] = new NetworkPolicerCriteria();
        this['exceed-action'] = new NetworkPolicerAction();
        this._inputValue = values;
        this.setValues(values, setDefaults);
    }

    /**
     * set the values for both the Model and the Form Group. If a value isn't provided and we have a default, we use that.
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    setValues(values: any, fillDefaults = true): void {
        if (values && values['_ui']) {
            this['_ui'] = values['_ui']
        }
        if (values) {
            this['criteria'].setValues(values['criteria'], fillDefaults);
        } else {
            this['criteria'].setValues(null, fillDefaults);
        }
        if (values) {
            this['exceed-action'].setValues(values['exceed-action'], fillDefaults);
        } else {
            this['exceed-action'].setValues(null, fillDefaults);
        }
        this.setFormGroupValuesToBeModelValues();
    }


    protected getFormGroup(): FormGroup {
        if (!this._formGroup) {
            this._formGroup = new FormGroup({
                'criteria': CustomFormGroup(this['criteria'].$formGroup, NetworkPolicerProfileSpec.propInfo['criteria'].required),
                'exceed-action': CustomFormGroup(this['exceed-action'].$formGroup, NetworkPolicerProfileSpec.propInfo['exceed-action'].required),
            });
            // We force recalculation of controls under a form group
            Object.keys((this._formGroup.get('criteria') as FormGroup).controls).forEach(field => {
                const control = this._formGroup.get('criteria').get(field);
                control.updateValueAndValidity();
            });
            // We force recalculation of controls under a form group
            Object.keys((this._formGroup.get('exceed-action') as FormGroup).controls).forEach(field => {
                const control = this._formGroup.get('exceed-action').get(field);
                control.updateValueAndValidity();
            });
        }
        return this._formGroup;
    }

    setModelToBeFormGroupValues() {
        this.setValues(this.$formGroup.value, false);
    }

    setFormGroupValuesToBeModelValues() {
        if (this._formGroup) {
            this['criteria'].setFormGroupValuesToBeModelValues();
            this['exceed-action'].setFormGroupValuesToBeModelValues();
        }
    }
}

