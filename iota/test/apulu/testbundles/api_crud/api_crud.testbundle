meta:
    name    : api_crud
    os      : [ linux ]
    nicmode : classic
    nics    : [ pensando ]

testcases:
    -   name    : Delete_And_Ping
        testcase: testcases.api_crud.ping
        stress  : True
        args    :
            type: remote_only
        iterators :
            type    : nested
            #ipaf   : [ ipv4, ipv6 ]
            ipaf    : [ ipv4 ]
            # objects tested with delete config.
            # TODO: nh-group
            # route object excluded from local2remote tests, because no hit
            # tunnel, nexthop object is excluded from delete operation as config is pushed by bgp and not iota
            #objtype : [ device, vpc, subnet, vnic, tunnel, lmapping, rmapping, nexthop, interface ]
            objtype : [ device, vpc, subnet, vnic, lmapping, rmapping ]
            oper    : [ [delete] ]

    -   name    : Update_And_Ping
        testcase: testcases.api_crud.ping
        stress  : True
        args    :
            type: remote_only
        iterators :
            type    : nested
            #ipaf   : [ ipv4, ipv6 ]
            ipaf    : [ ipv4 ]
            # objects tested with update config.
            # TODO: nh-group
            # nexthop object is excluded from update operation as config is pushed by bgp and not iota
            #objtype : [ device, vpc, subnet, vnic, tunnel, nexthop, interface]
            objtype : [ device, vpc, subnet, vnic, interface]
            oper    : [ [update] ]

    -   name    : Create_Create_And_Ping
        testcase: testcases.api_crud.ping
        stress  : True
        args    :
            type: remote_only
        iterators :
            type    : nested
            #ipaf   : [ ipv4, ipv6 ]
            ipaf    : [ ipv4 ]
            # TODO: nh-group
            objtype : [ device, vpc, subnet, vnic, tunnel, interface, lmapping, rmapping ]
            oper    : [ [create, create] ]

    -   name    : Delete_Update_And_Ping
        testcase: testcases.api_crud.ping
        stress  : True
        args    :
            type: remote_only
        iterators :
            type    : nested
            #ipaf   : [ ipv4, ipv6 ]
            ipaf    : [ ipv4 ]
            # TODO: nh-group
            #objtype : [ device, vpc, subnet, vnic, nexthop, tunnel, interface, lmapping, rmapping ]
            objtype : [ device, vpc, subnet, vnic, lmapping, rmapping ]
            oper    : [ [delete, read, update, read] ]

    -   name    : Delete_Delete_And_Ping
        testcase: testcases.api_crud.ping
        stress  : True
        args    :
            type: remote_only
        iterators :
            type    : nested
            #ipaf   : [ ipv4, ipv6 ]
            ipaf    : [ ipv4 ]
            #objtype : [ device, vpc, subnet, vnic, nexthop, tunnel, interface, lmapping, rmapping ]
            objtype : [ device, vpc, subnet, vnic, lmapping, rmapping ]
            oper    : [ [delete, delete] ]

    -   name    : Update_Create_And_Ping
        testcase: testcases.api_crud.ping
        stress  : True
        args    :
            type: remote_only
        iterators :
            type    : nested
            #ipaf   : [ ipv4, ipv6 ]
            ipaf    : [ ipv4 ]
            objtype : [ device, vpc, subnet, vnic, interface, lmapping, rmapping ]
            oper    : [ [update, create] ]

